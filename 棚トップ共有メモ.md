# PATTI SHOT ── 棚トップ統合用 共有メモ

> **用途**：PATTI CREATIVE PRODUCT トップページを作っているセッションへの引き渡し資料。
> このファイルだけ読めば PATTI SHOT の扱いが分かるように、事実だけを書いています。
> **最終更新：2026-07-30**（拡張機能 v5.5.1 / アプリ版 v4.2.3 時点。全URLの生存を実測確認済み）

> ⚠ 依頼にあった「③」が空欄だったので、統合時にいちばん必要になりそうな
> **「③ 導入と更新のしかた（利用者向け）」** を入れています。別の内容をご希望なら差し替えます。

---

## 0. 基本情報（コピペ用）

| 項目 | 値 |
|---|---|
| プロダクト名 | **PATTI SHOT** |
| 一言 | Webページを、スクロールしないと見えない一番下まで丸ごと1枚のPNG / PDFに保存 |
| プロダクトページ | https://ikemotodir.github.io/patti-shot/ |
| リポジトリ | https://github.com/ikemotodir/patti-shot |
| プライバシーポリシー | https://ikemotodir.github.io/patti-shot/privacy.html |
| ロゴ | `docs/assets/patti_shot_logo.png`（カメラを持ったおばけ） |
| ブランドカラー | ピンク `#D6336C` / アクセント黄 `#FFC400` / 背景 `#1a1a24` |
| 制作 | STUDIO PATTI（合同会社スタジオパッチ） |
| 料金 | 無料・登録不要 |

---

## ① 現状

### 配布形態は2つある（ここが他プロダクトと違う最重要ポイント）

| | **Chrome拡張版**（主・推奨） | **アプリ版 (exe)**（副） |
|---|---|---|
| 版数 | **v5.5.1** | **v4.2.3** |
| 公開場所 | GitHub Releases `ext-v5.5.1` | GitHub Releases `v4.2.3` |
| 形式 | zip（展開して読み込み） | 単体exe（インストール不要） |
| 動くブラウザ | いつものChrome本体 | 専用ブラウザが別ウィンドウで開く |
| ログイン中のページ | **そのまま撮れる** | PATTI SHOT側で初回だけログインが必要 |
| 自動更新 | ✕（通知のみ・手動入替） | **○ 全自動**（DL→置換→再起動） |
| ウイルスソフト警告 | **出ない** | 出ることがある（②参照） |
| 状態 | 一般公開中 | 公開中（旧・併記扱い） |

### 立ち位置の説明（なぜ2つあるか）
もともとexe版が先。「いま見ているページを撮りたい」「ログインし直したくない」という要望に
**Chromeの仕様上exeでは応えられない**（Chrome 136+ が外部プログラムからの既定プロファイル接続を禁止）ため、
**Chrome拡張版に作り直した**という経緯。今は**拡張版が主**、exe版は「専用ブラウザで使いたい人向け」の併記。

### Chromeウェブストア：未提出（準備完了・審査待ちですらない）
- 手順書とストア掲載素材（スクリーンショット2枚1280×800／プロモ440×280／権限説明文）は**準備済み**
- 未着手の理由：**$5の登録料の支払いが必要**で、カード入力は池本さん本人しかできないため
- 通れば「ボタン1つで導入・全自動更新・デベロッパーモード不要」になる
- **棚トップに書くなら「Chromeウェブストア版は準備中」程度に留めるのが安全**（審査期間が読めないため）

### 既知の制限（正直に書いておく）
数万ピクセル級の**特に長いページ**で、一部の環境（PC・表示倍率の組み合わせ）だと
**貼り合わせが乱れる（同じ行が二度出る）ことがある**。
- 撮影後にツール自身が画素で検算し、確定できない場合は**保存時の通知とログ（`_log.txt`）で明示**する
- 通常の長さのページでは発生しない
- **棚トップに書く必要はない**（プロダクトページ側にだけ記載済み）。ただし「完璧です」と断言する表現は避けたい

---

## ② ウイルス対策ソフトへの対応（Avast以外も）

### 前提：拡張版は無関係。この話はアプリ版(exe)だけ
Chrome拡張版はChromeが管理するため、**ウイルスソフトの警告は出ません**。
棚トップで両方を紹介する場合、**「exe版のみ注意書きあり」**という整理になります。

### なぜ出るのか（利用者向けの説明文・そのまま使えます）
```
PATTI SHOT のアプリ版は「署名のない個人開発アプリ」です。
そのため初回起動時に Windows やウイルス対策ソフトが警告を出すことがあります。
これは「作者不明の新しいアプリ」に対する一般的な反応で、
ウイルスが検出されたわけではありません。
ソースコードは GitHub で全公開しており、誰でも中身を確認できます。
```

技術的な背景：PyInstaller製のexeは、実行時に自分自身を一時フォルダへ展開して動く構造のため、
**マルウェアと似た振る舞いに見える**（＝業界的に有名な誤検知パターン）。

### 対応方法（ソフト別・共通の考え方）

**共通の原則：ファイル単位ではなく「フォルダごと」除外する。**
自動更新でexeが新しいファイルに置き換わるため、ファイル単位の除外だと更新のたびに効かなくなります。
（※これは池本さんのPCで実際に起きた事象。フォルダ除外に変更して解決済み）

| ソフト | 手順 |
|---|---|
| **Windows Defender**（標準） | 設定 → プライバシーとセキュリティ → Windows セキュリティ → ウイルスと脅威の防止 → 「設定の管理」→ 除外の「除外の追加または削除」→ **フォルダー** を選択 |
| **Avast / AVG** | メニュー → 設定 → 全般 → 例外 → 例外を追加 → **「参照」からフォルダを選ぶ**<br>※パスを手入力する方式だと起動時にエラーになる事象を確認済み。必ず「参照」で選ぶこと |
| **ESET** | 詳細設定 (F5) → 検出エンジン → 除外 → 除外するパス → 追加 → フォルダを指定 |
| **Norton** | 設定 → ウイルス対策 → スキャンとリスク → 「スキャンから除外する項目」／「自動保護から除外する項目」の両方に追加 |
| **McAfee** | リアルタイムスキャン → 設定 → 除外されたファイル → ファイル追加 |
| **ウイルスバスター** | 設定 → 例外設定 → ファイル／フォルダ → 追加 |

### Windows SmartScreen（ウイルスソフトとは別物）
「WindowsによってPCが保護されました」が出た場合：
**「詳細情報」→「実行」** の順にクリック。署名のない個人開発アプリで必ず出る画面。

### 棚トップに載せるなら（短縮版）
```
※アプリ版(exe)は署名のない個人開発アプリのため、初回起動時に
　Windowsやウイルス対策ソフトが警告を出すことがあります（詳しくは製品ページへ）。
　Chrome拡張版では出ません。
```

---

## ③ 導入と更新のしかた（利用者向け）※③が空欄だったため補完

### Chrome拡張版（推奨）
**導入（初回だけ・1分）**
1. プロダクトページから zip をダウンロードして展開
2. フォルダごと**動かさない場所**に置く（例：ドキュメントの中）
3. Chrome で `chrome://extensions` → 右上「デベロッパー モード」を ON
4. 「パッケージ化されていない拡張機能を読み込む」→ 2のフォルダを選ぶ
5. 撮りたいページの右下にピンクのボタン → 押すだけ（`Ctrl + Shift + S` でも可）

**更新**
- 新しい版が出ると、ツールバーのアイコンを押した画面に「新しい版があります」と表示
- zip をDL → **フォルダの中身を入れ替え** → `chrome://extensions` の「更新」ボタン
- ⚠ **Chromeの仕様で、ストア外の拡張機能は自動更新できません**（開発者側の都合ではなく仕様）
- 同梱の `create_shortcut.bat` で、拡張フォルダへのショートカットをデスクトップに作れる

### アプリ版 (exe)
**導入**：exe をダウンロード → ダブルクリック（インストール作業なし）
**更新**：起動時に自動チェック → 「更新する」を押すだけで **DL→置換→再起動まで全自動**
（デスクトップのショートカットからの起動でも、更新後は最新版が開きます）
**ショートカット**：アプリ内の歯車⚙ →「デスクトップにショートカット作成」

### 保存先（共通）
`ダウンロード \ PATTI SHOT` フォルダ。PNG / PDF / ログ（`_log.txt`）が保存されます。

---

## ④ プロダクトページの内容（現在の構成）

URL: https://ikemotodir.github.io/patti-shot/ （稼働確認済み・200）

### セクション構成（上から順）
1. **ヘッダー**：ロゴ＋キャッチコピー＋**「⬇ Chrome拡張版をダウンロード（v5.5.1）」**（主ボタン）
   ＋「アプリ版(exe)はこちら（v4.2.3）」（副ボタン・グレー）
2. **完全ローカルの安心**：撮影データは外部送信なし／保存先の案内
3. **🧩 Chrome拡張版の入れ方と更新**：5ステップ＋更新の仕組み＋`create_shortcut.bat`の説明
4. **📸 こんなことができる**：4カード（一番下まで1枚／PNG・PDF／ログイン中もOK／ボタン1つ）
5. **🚀 アプリ版(exe)のはじめかた**：3ステップ＋SmartScreen注意
6. **⚡ いま見ているページをそのまま開く**：exe版向けのブックマークレット／クリップボード連携
7. **🛡 セキュリティソフトの警告について**：②の内容（Avast手順を含む）
8. **⚙ 設定**：保存形式・画質・ショートカット作成
9. **🔃 アップデート**：拡張版とexe版それぞれの更新方法
10. **📣 SPONSOR**：棚トップと同一仕様の広告枠（`#sponsor-box`）
11. **❓ よくある質問**：10項目（送信されない／動作環境／SmartScreen／誤検知／ログイン／長さ／失敗時／Mac／商用利用／なぜ無料）
12. **免責**：3項目
13. **フッター**：`Chrome拡張版 v5.5.1 ／ アプリ版 v4.2.3`

### 技術的に共有しておきたい仕掛け
- **版数とダウンロードURLは GitHub Releases API から自動取得**している
  （`fetch('https://api.github.com/repos/ikemotodir/patti-shot/releases?per_page=30')` →
  `ext-v*` タグの最新＝拡張版、`v数字` タグ＋`PATTI_SHOT.exe`アセット＝exe版）
  → **リリースを出すだけでページの版数表示とDLリンクが追従する**。手で書き換える箇所はゼロ
  → 棚トップでも同じ仕掛けを使えば、版数の書き換え作業を無くせます
- GA4 導入済み（`G-C9RFZES8ZS`）。DLボタンに `gtag('event','download',{app:'patti-shot'})` /
  拡張版は `app:'patti-shot-ext'`
- OGP・Twitterカード設定済み
- スポンサー枠は**棚トップの `#sponsor-box` と同一仕様**（棚側を変えたらこちらも追従させる約束）

---

## ⑤ PATTI CREATIVE PRODUCT トップページへの反映

### 現在の掲載内容（2026-07-30時点・実測）
- ステータス：**公開中**
- 説明文：`Webページを一番下まで丸ごと1枚のPNG / PDFに保存。AIにまるごと読ませたい時にも。`
- リンク：https://ikemotodir.github.io/patti-shot/
- 掲載順：SCRIBE → **SHOT** → CROP → PIXEL → MINIATURE

### 変更が必要な点

**1. 対応環境の表記**
現在は Windows専用アプリという前提の可能性あり。実際は：
```
Chrome拡張版：Windows / Mac / Linux（Chromeが動けばOK）
アプリ版(exe)：Windows 10 / 11 専用
```
→ **拡張版になったことで Mac でも使えるようになった**のが、棚トップ的に一番大きな更新点です。

**2. 説明文（現行のままでも通用しますが、拡張版であることを出すなら）**
```
案A（現行維持・無難）
Webページを一番下まで丸ごと1枚のPNG / PDFに保存。AIにまるごと読ませたい時にも。

案B（拡張版を前面に）
Chromeに入れて、いま見ているページを一番下まで丸ごと1枚に。ログイン中のページもそのまま。

案C（短く強く）
いま見ているWebページを、一番下まで丸ごと1枚のPNG / PDFに。
```

**3. バッジ／補足表記の候補**
- `Chrome拡張` バッジを付けると、他プロダクト（exe配布）との違いが一目で分かります
- 「Chromeウェブストア版 準備中」は**書かない方が安全**（審査期間が読めないため）。
  書くとしても控えめに

**4. 版数を出す場合**
上記④の Releases API の仕掛けを使えば自動追従できます。手書きなら：
`Chrome拡張版 v5.5.1 ／ アプリ版 v4.2.3`

**5. ウイルスソフト注意書き**
棚トップに載せるなら②末尾の短縮版を。ただし**拡張版が主になった今、
棚トップでは触れずプロダクトページに任せる**のが自然です。

### 他プロダクトと横並びで見たときの注意
- SHOT だけ**配布形態が2つ**あるので、カードに詰め込みすぎないこと。
  カードは「拡張版＝主」で1本化し、exe版はプロダクトページ側で説明する形が読みやすいはずです
- SHOT だけ **Mac対応**（拡張版）なので、棚トップに対応OS表記があるなら例外扱いになります

---

## 参考：ファイルの置き場所

| 内容 | パス |
|---|---|
| 拡張機能の中身 | `C:\Users\studi\Desktop\PATTI SHOT 拡張機能`（＝GitHubの `extension/`） |
| 配布用zip | `C:\Users\studi\Desktop\Claude\apps\patti-shot\build\patti_shot_extension_v5.5.1.zip` |
| ストア掲載素材 | `...\build\store\`（スクショ2枚＋プロモ440×280） |
| ストア公開手順書 | `C:\Users\studi\Desktop\PATTI SHOT_ストア公開手順.md` |
| プロダクトページ | GitHubリポジトリの `docs/index.html` |
